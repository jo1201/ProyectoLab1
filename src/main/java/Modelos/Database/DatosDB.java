
package Modelos.Database;

public class DatosDB {
    
    protected String database = "GestionSocial",
                          url = "jdbc:postgresql://localhost:5432/" + database,
                         user = "admin",
                         pass = "admin";
}
